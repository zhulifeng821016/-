name: 打包测试
on:
workflow_dispatch:
jobs:
build:
runs-on: ubuntu-latest
steps:
uses: actions/checkout@v4
name: 设置 Node.js
uses: actions/setup-node@v4
with:
node-version: '20'
name: 安装依赖并打包
run: |
npm install
npm run build
